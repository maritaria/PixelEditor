local canvas = require("screen"):createClass()

function canvas:onMousePressed(x, y, button)
end

function canvas:onMouseMoved(x, y, button)
end

function canvas:onMouseReleased(x, y, button)
end

return canvas
